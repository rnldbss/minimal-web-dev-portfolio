

export default function ButtonPrimary({buttonHref, buttonText}) {
  return  (<a
  class="px-5 py-px rounded-sm inline-block bg-accent-primary text-on-level-0-dark w-max"
  href={buttonHref}>{buttonText}</a>)
}
 
   

